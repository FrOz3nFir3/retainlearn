{
  "name": "retainlearn",
  "version": "1.0.0",
  "description": "A full-stack spaced repetition and flashcard application built with React and Node.js.",
  "scripts": {
    "install-all": "npm install && npm install --prefix ./client && npm install --prefix ./server",
    "install-server": "npm install --prefix server",
    "install-client": "npm install --prefix client",
    "server": "npm run start --prefix server",
    "client": "npm run build --prefix client",
    "server-dev": "npm run watch --prefix server",
    "client-dev": "npm run watch --prefix client",
    "dev": "concurrently \"npm:server-dev\" \"npm:client-dev\"",
    "deploy": "npm run client && npm run server"
  },
  "keywords": [],
  "author": "FrOz3nFir3",
  "license": "Unlicense",
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
